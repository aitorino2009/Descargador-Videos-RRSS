version: '3.8'

services:
  clipprofit-downloader:
    build: .
    container_name: clipprofit_web
    ports:
      - "7432:7432"
    environment:
      - HOSTED=true
      - PORT=7432
      - NODE_ENV=production
    restart: unless-stopped
